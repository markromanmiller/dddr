finite_difference <- function(points, times=NULL, method=c("forward", "backward")) {
  stop("Not Implemented Yet")
}

finite_accumulant <- function(points, times=NULL, method=c("forward", "backward")) {
  stop("Not Implemented Yet")
}
